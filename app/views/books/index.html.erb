<h1>Books</h1>

<h1>投稿一覧</h1>

<% @book_all.each do |book| %>

  <p>タイトル</p>
  <span><%= book.title %></span>
  
<% end %>


<%= form_with model: @book do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
	
  <h4>感想</h4>
  <%= f.text_area :body %>
	
  <%= f.submit '投稿' %>
<% end %>